# Stable Diffusion files

Obtain the Stable Diffusion v1.5 checkpoint `v1-5-pruned-emaonly.ckpt` and the tokenizer files `vocab.json` and `merges.txt` from their authorized provider. These third-party files are not redistributed.
