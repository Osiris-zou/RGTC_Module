# Checkpoint preparation

The scripts can use timm pretrained weights (`--pretrained`) or an explicit checkpoint (`--checkpoint`). Checkpoints are not redistributed. The model name and preprocessing choice must match the checkpoint used in the paper.
